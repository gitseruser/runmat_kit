# runmat_kit
Recompiled runmat WASM engine + web GUI. Debugger included. Hobby project.

You should use an local http server to run this kit.
Its NOT WORKING IN FILE ACCESS MODE in browser.

WASM engine changed and compiled. No sources.
web GUI created by author

Also unzip archive assets/runmat_wasm_gpu_bg.7z ==> runmat_wasm_gpu_bg.wasm
its bigger than 25 MB so i could not upload it unzipped.
